<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roowoo.log.modules.terminal.dao.LoginTerminalDao">
    
	<sql id="loginTerminalColumns">
		a.id AS "id",
		a.login_time AS "loginTime",
		a.terminal_ip AS "terminalIp",
		a.account_number AS "accountNumber",
		a.visit_number AS "visitNumber",
		a.manu_visit_number AS "manuVisitNumber",
		a.travel_visit_number AS "travelVisitNumber",
		a.study_visit_number AS "studyVisitNumber",
		a.win_number AS "winNumber",
		a.fail_number AS "failNumber"
	</sql>
	
	<sql id="loginTerminalJoins">
	</sql>
    
	<select id="get" resultType="LoginTerminal">
		SELECT 
			<include refid="loginTerminalColumns"/>
		FROM login_terminal a
		<include refid="loginTerminalJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="LoginTerminal">
		SELECT 
			<include refid="loginTerminalColumns"/>
		FROM login_terminal a
		<include refid="loginTerminalJoins"/>
		<where>
			
			<if test="beginLoginTime != null and endLoginTime != null">
				AND a.login_time BETWEEN #{beginLoginTime} AND #{endLoginTime}
			</if>
			<if test="terminalIp != null and terminalIp != ''">
				AND a.terminal_ip = #{terminalIp}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="LoginTerminal">
		SELECT 
			<include refid="loginTerminalColumns"/>
		FROM login_terminal a
		<include refid="loginTerminalJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO login_terminal(
			id,
			login_time,
			terminal_ip,
			account_number,
			visit_number,
			manu_visit_number,
			travel_visit_number,
			study_visit_number,
			win_number,
			fail_number
		) VALUES (
			#{id},
			#{loginTime},
			#{terminalIp},
			#{accountNumber},
			#{visitNumber},
			#{manuVisitNumber},
			#{travelVisitNumber},
			#{studyVisitNumber},
			#{winNumber},
			#{failNumber}
		)
	</insert>
	
	<update id="update">
		UPDATE login_terminal SET 	
			login_time = #{loginTime},
			terminal_ip = #{terminalIp},
			account_number = #{accountNumber},
			visit_number = #{visitNumber},
			manu_visit_number = #{manuVisitNumber},
			travel_visit_number = #{travelVisitNumber},
			study_visit_number = #{studyVisitNumber},
			win_number = #{winNumber},
			fail_number = #{failNumber}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM login_terminal
		WHERE id = #{id}
	</update>
	
</mapper>