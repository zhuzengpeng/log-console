<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roowoo.log.modules.count1.dao.Count1lDao">
    
	<sql id="count1lColumns">
		a.depart_id AS "departId",
		a.failcount AS "failcount",
		a.successcount AS "successcount"
	</sql>
	
	<sql id="count1lJoins">
	</sql>
    
	<select id="get" resultType="Count1l">
		SELECT 
			<include refid="count1lColumns"/>
		FROM count1l a
		<include refid="count1lJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Count1l">
		SELECT
			<include refid="count1lColumns"/>
		FROM count1l a
		<include refid="count1lJoins"/>
		<where>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Count1l">
		SELECT 
			<include refid="count1lColumns"/>
		FROM count1l a
		<include refid="count1lJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO count1l(
			depart_id,
			failcount,
			successcount
		) VALUES (
			#{departId},
			#{failcount},
			#{successcount}
		)
	</insert>
	
	<update id="update">
		UPDATE count1l SET 	
			depart_id = #{departId},
			failcount = #{failcount},
			successcount = #{successcount}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM count1l
		WHERE id = #{id}
	</update>

	<select id="findListByParentId" resultType="Count1l">
		select * from count1l a where a.parentid=0
	</select>

	<select id="findListByParentIdSon" resultType="Count1l">
		select * from count1l a where a.parentid=#{departid}
	</select>
</mapper>